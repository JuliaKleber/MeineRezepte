/*import AddIngredients from "./AddIngredients";
import AddAmounts from "./AddAmounts";
import AddKeywords from "./AddKeywords";*/

/*  <AddKeywords
          onChangeStep={handleCurrentStep}
          recipeName={recipeName}
          amounts={amounts}
          ingredients={chosenIngredients}
          description={description}
          keywords={keywords}
        /> */

/*
  // Geht zum Start Menü
  function goToStartMenu() {
    props.onReturnHome(true);
    setCurrentStep("none");
  }

  // Geht zur Auswahl der Zutaten
  function goToIngredients() {
    setCurrentStep("ingredients");
  }

  // Geht zu der Eingabe der Mengen der Zutaten
  function goToAmounts() {
    setCurrentStep("amounts");
  }  
  
  function goToRecipeAdded() {
    setRecipeName("");
    setChosenIngredients([]);
    setAmounts([]);
    setKeywords([]);
    setDescription("");
    setCurrentStep("recipeAdded");
  }

  function goToRecipeNotAdded() {
    setCurrentStep("recipeNotAdded");
  }
  
  // Speichert die vom Nutzer eingegebenen Mengen der Zutaten.
  function goToKeywords() {
    if (recipeName === "") {
      recipeNameInputRef.current.focus();
    } else {
      setKeywords([...keywords, recipeName]);
      setCurrentStep("keywords");
    }
  }*/
